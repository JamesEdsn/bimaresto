info:
  name: Delete Promo
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/promos/{{last_promo_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
