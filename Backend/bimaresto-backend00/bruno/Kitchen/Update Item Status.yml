info:
  name: Update Item Status
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base_url}}/kitchen/items/1/status"
  body:
    type: json
    data: |-
      {
        "status": "cooking"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5