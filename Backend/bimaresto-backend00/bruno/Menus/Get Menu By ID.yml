info:
  name: Get Menu By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/menus/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5