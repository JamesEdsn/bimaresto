info:
  name: Get Categories
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/categories"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5