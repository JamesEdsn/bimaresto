info:
  name: Update Category
  type: http
  seq: 8

http:
  method: PUT
  url: "{{base_url}}/categories/1"
  body:
    type: json
    data: |-
      {
        "name": "Makanan Utama"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5