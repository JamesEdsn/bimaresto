info:
  name: Delete Menu
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/menus/{{last_menu_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5