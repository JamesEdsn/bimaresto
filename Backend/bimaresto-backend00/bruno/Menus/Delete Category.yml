info:
  name: Delete Category
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/categories/{{last_category_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5