info:
  name: Get Menus
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/menus?page=1&limit=100"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5