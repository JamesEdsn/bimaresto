info:
  name: Status
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/api/tables/2/status
  body:
    type: json
    data: |-
      {
        "status": "occupied"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
