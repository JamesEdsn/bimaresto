info:
  name: Status
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base_url}}/tables/2/status"
  body:
    type: json
    data: |-
      {
        "status": "occupied"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
