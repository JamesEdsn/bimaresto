info:
  name: Get Roles
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/roles"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5