info:
  name: Refresh Token
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/refresh"
  body:
    type: json
    data: |-
      {
        "refresh_token": "{{refresh_token}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5