info:
  name: Delete Staff
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/staff/{{last_staff_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5