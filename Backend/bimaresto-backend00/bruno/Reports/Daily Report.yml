info:
  name: Daily Report
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/reports/daily?days=30"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5