info:
  name: Cancel Order
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/orders/{{last_order_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5