info:
  name: Delete Payment
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{base_url}}/payments/{{last_payment_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5