info:
  name: Generate Bill
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/orders/1/bill"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
