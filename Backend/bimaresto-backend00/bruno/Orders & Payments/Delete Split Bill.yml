info:
  name: Delete Split Bill
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{base_url}}/splitbills/{{last_splitbill_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5