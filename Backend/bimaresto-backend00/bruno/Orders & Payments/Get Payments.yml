info:
  name: Get Payments
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/payments"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5